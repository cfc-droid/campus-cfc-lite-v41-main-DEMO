# =========================================================
# ✅ CFC-SYNC V8.2 — Rutas limpias + Login.html habilitado
# =========================================================

# Módulos
/modules/*          /modules/:splat            200

# Archivos estáticos (CSS, JS, IMG)
#/js/*               /js/:splat                 200
#/css/*              /css/:splat                200
#/img/*              /img/:splat                200
/frontend/*          /frontend/:splat           200

# Rutas HTML específicas
/frontend/html/*     /frontend/html/:splat      200

# Archivos raíz (asegurar carga correcta del service worker, manifest, etc)
#/sw.js              /sw.js                     200
#/manifest.json      /manifest.json             200

# Página única SPA (solo para el Campus principal)
 /*                   /index.html                200
